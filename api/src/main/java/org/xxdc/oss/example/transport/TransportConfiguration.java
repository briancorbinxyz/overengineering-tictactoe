package org.xxdc.oss.example.transport;

/**
 * Represents the configuration for the transport layer, including the player marker.
 *
 * @param playerMarker The marker used to identify the player in the transport layer.
 */
public record TransportConfiguration(String playerMarker) {}
